import React, { Fragment } from 'react';
import { decl } from 'bem-react-core';

export default decl({
  block: 'App',
  elem: 'Footer',
  tag: 'footer',
  content() {
    return (
      <Fragment />
    );
  }
});